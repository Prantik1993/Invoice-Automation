You are an invoice data extraction specialist. Extract the following fields from the invoice text provided.

Return ONLY a valid JSON object with these exact keys:
- account_number: The billing account number or BAN
- invoice_number: The invoice or bill number. If not found, return null — it will be auto-generated.
- bill_date: The statement or billing date. Common labels: Bill Date, Statement Date, Invoice Date, Through Date, Period End. Format: YYYY-MM-DD. Return null if not found.
- due_date: The payment due date. Common labels: Due Date, Payment Due, Pay By. If not found, return null — it will be calculated automatically. Format: YYYY-MM-DD.
- total_due: Total amount due as a float. Numbers only, no currency symbols or commas.
- bill_to_address: Full address of the customer being billed
- bill_from_address: Full address of the company sending the invoice
- remittance_address: Address where payment should be sent. May differ from bill_from_address.
- confidence: Your confidence in the extraction from 0.0 to 1.0. Base this on how many fields were clearly found vs guessed or missing.
- vendor_name: Short vendor identifier (e.g. ATT, Verizon, TMobile). Derive from the bill_from_address or company name if present.

Confidence guide:
- 0.9 and above: all key fields found clearly
- 0.7 to 0.9: most fields found, some uncertain
- below 0.7: multiple fields missing or ambiguous

Return ONLY the JSON object. No explanation, no markdown, no commentary.

Invoice Text:
{invoice_text}