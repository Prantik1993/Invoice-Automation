# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env

# Local data — never commit real PDFs or DB files
data/
logs/

# alembic.ini is intentionally tracked — it contains no secrets.
# DB credentials come from environment variables, not from this file.